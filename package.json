{
  "name": "documind",
  "version": "1.0.0",
  "description": "DocuMind — a MERN-stack app to chat with your PDFs using Google Gemini AI.",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "backend": "npm --prefix backend run start",
    "frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n backend,frontend -c blue,magenta \"npm run backend\" \"npm run frontend\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
